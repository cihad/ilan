<div class="row presentation">
  <% @catalog.each do |column| %>
    <div class="col-sm-<%= 12 / @catalog.size %> col-xs-12">
      <% column.each do |item| %>
        <%= render partial: item.class.name.underscore, locals: { item: item } %>
      <% end %>
    </div>
  <% end %>
</div>